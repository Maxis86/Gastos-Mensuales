import { GASTOS_MES } from "../../types";

export default (state, action) => {
    switch (action.type) {
        case GASTOS_MES:
            
            break;
    
        default:
            break;
    }
}
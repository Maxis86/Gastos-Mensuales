import { 
    REGISTRO_EXISTOSO,
    REGISTRO_ERROR,
    OBTENER_USUARIO,
    LOGIN_ERROR,
    LOGIN_EXITOSO,
    
 } from "../../types";

export default (state, action) => {
    
    switch(action.type){
      
        default:
            return state;
    }
}